{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    {% if admin.datagrid.results|length >= 1 %}
        <div>
            <strong>{{ object.getNbNews}}</strong>
        </div>
    {% endif %}
{% endblock %}
